{
  "name": "@truongnat/skill-cli",
  "version": "1.0.0",
  "bin": {
    "skill": "./dist/skill"
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/skill --target node --compile",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "conf": "^13.0.0",
    "marked": "^12.0.0",
    "marked-terminal": "^7.0.0",
    "ora": "^8.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.1.3"
  }
}
